================================================================================
  AQUA — Activar registro / login con Google
================================================================================

1) CONSOLA DE GOOGLE CLOUD
   - https://console.cloud.google.com/
   - Creá un proyecto (o usá uno existente).
   - APIs y servicios → Pantalla de consentimiento de OAuth
     * Tipo: Externo (o Interno si es solo tu organización)
     * Completá nombre de app, email de soporte y dominios si tenés web pública.
   - APIs y servicios → Credenciales → Crear credenciales → ID de cliente OAuth
     * Tipo de aplicación: Aplicación web

2) URIs DE REDIRECCIÓN (obligatorio)
   Agregá exactamente estas URLs (una por línea):

   Producción:
     https://TU-DOMINIO/api/auth/callback/google

   Desarrollo local:
     http://localhost:3000/api/auth/callback/google

   (Reemplazá TU-DOMINIO por tu URL de Railway, sin barra final)

3) VARIABLES EN .env Y RAILWAY

   GOOGLE_CLIENT_ID="xxxxx.apps.googleusercontent.com"
   GOOGLE_CLIENT_SECRET="GOCSPX-xxxxxxxx"
   NEXTAUTH_URL="https://TU-DOMINIO"    (o http://localhost:3000 en local)
   NEXTAUTH_SECRET="..."                (ya deberías tenerlo)

   Reiniciá / redeployá el servicio después de guardar.

4) COMPORTAMIENTO EN AQUA

   - En /registro y /login aparece "Continuar con Google" si las variables están configuradas.
   - Primera vez con Google: se crea cuenta CLIENTE con email verificado (puede comprar al toque).
   - Si el email ya existe como cliente: inicia sesión y marca email verificado si faltaba.
   - Cuentas de empleado/dueño (staff): NO pueden entrar con Google; deben usar email + contraseña + clave de sucursal.

5) PROBAR EN LOCAL

   - GOOGLE_CLIENT_ID y GOOGLE_CLIENT_SECRET en .env
   - NEXTAUTH_URL=http://localhost:3000
   - npm run dev
   - Ir a /registro → Registrarse con Google

6) ERRORES FRECUENTES

   - redirect_uri_mismatch → la URI en Google no coincide con NEXTAUTH_URL + /api/auth/callback/google
   - Configuration → faltan GOOGLE_CLIENT_ID o GOOGLE_CLIENT_SECRET
   - AccessDenied en /login → intentaste Google con un email de staff

================================================================================
